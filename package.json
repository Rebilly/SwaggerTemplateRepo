{
  "dependencies": {
    "bower": "^1.7.7",
    "deploy-to-gh-pages": "0.0.1",
    "swagger-repo": "IvanGoncharov/swagger-repo"
  },
  "private": true,
  "scripts": {
    "deploy": "./scripts/deploy.sh && deploy-to-gh-pages portal_deploy",
    "swagger": "swagger-repo",
    "test": "swagger-repo validate"
  }
}
